
<%- include('partials/header') %>
    <h1>All Products</h1>

    <div id="allCards">
        <% products.forEach(product => { %>
            <div class="productCard" id="<%=product.product_id%>">
                <h2><%= product.product_name %></h2>
            </div>
        <% }) %>
    </div>

<%- include('partials/footer') %>